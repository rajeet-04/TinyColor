---
phase: 05-delivery-evidence
plan: 2
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-08-01T00:04:30.289Z
branch: rajeet
---

<current_state>

Phase 5 Plan 05-01 is implemented and CI-verified. Plan 05-02 Task 1 is paused
mid-debug: the broad persistent differential harness is implemented, but its
zero-divergence test correctly remains RED because fuzzing found exact port
defects. Do not narrow the generator or add comparison tolerance.

The branch is intentionally being handed off with WIP files. The first fuzz
defect (`saturate(-100)` rounding) is fixed and committed. The next defect is
an exact WCAG luminance/readability precision mismatch under active diagnosis.

</current_state>

<completed_work>

- Phase 5 design, context, research, validation, and three execution plans were
  written and independently plan-checked.
- Human CLI commands `parse`, `convert`, `lighten`, `palette`, and `contrast`
  were implemented with JSON output while preserving zero-argument JSONL mode.
- Added one-command Make build/verify, oracle hash verification, scratch Docker
  artifact, and GitHub Actions delivery workflow.
- Fixed CI raw-byte hash failures by pinning the three oracle files to CRLF in
  `.gitattributes`; a simulated `core.autocrlf=false` checkout prints
  `verified: 3`.
- Fixed Deno 2 discovery by running the immutable suite explicitly with
  `deno test test.js`.
- GitHub Actions run
  `https://github.com/rajeet-04/TinyColor/actions/runs/30673847990` passed in
  42 seconds: `make verify`, Deno source suite, build, and artifact upload.
- Local Deno 2.9.4 source suite result: 45 passed, 0 failed, 1 ignored.
- Built the standard-library persistent fuzz harness with seeded broad inputs,
  one outstanding request per child, protocol safeguards, and RED/GREEN tests.
- Fixed the first fuzz mismatch in `0a9db8b`: TinyColor HSL percentage
  truncation for `saturate(-100)` now matches; operations corpus is 70/70 at
  that commit.

</completed_work>

<remaining_work>

1. Finish Task 05-02-01:
   - Continue `.planning/debug/fuzz-readability-precision.md`.
   - Existing RED case: `{r:127,g:64,b:15,a:0}` vs `#80007f` returns
     JavaScript `1.188086751976723`, Go `1.1880867519767233`.
   - Root cause is confirmed inside nonlinear luminance channel expansion:
     V8 `Math.pow` and Go `math.Pow` differ in final bits for 93 of the 245
     nonlinear 8-bit channels; two differ by two ULPs.
   - `src/tinycolor/operations_test.go` and
     `compat/cases/operations.jsonl` contain the failing exact regression.
   - `src/cmd/pow-debug/main.go` is a temporary diagnostic that prints Go
     channel power bits.
   - Leading safe fix: a reproducibly generated exact 256-entry V8 transfer
     lookup used by `Color.Luminance`. Do not use `Nextafter`, decimal output
     rounding, or tolerance.
   - After the exact regression and 71-row operations corpus pass, rerun
     `node --test fuzz/harness.test.mjs` and
     `node fuzz/harness.mjs --duration 1 --seed 1` broadly. If another
     mismatch appears, repeat deterministic regression -> root cause -> micro
     commit.
2. Task 05-02-02: add tested fuzz-log validator and record an actual
   `--duration 60 --seed 20260801` zero-divergence run.
3. Task 05-02-03: implement shared workload benchmark runner and publish actual
   p99 latency, peak RSS, startup, and throughput results without quick-run
   overwrite.
4. Plan 05-03: rewrite judge-facing README, expand DECISIONS to 12+ substantive
   rows, refresh COMPATIBILITY with exact evidence/coverage/unsafe counts, add
   demo script, verify public clone/current CI, and close roadmap/state.
5. Human: record and publish the five-minute demo video; no video is currently
   claimed.

</remaining_work>

<decisions_made>

- Broad fuzzing stays broad. A mismatch blocks the bonus and becomes a fixed
  regression; inputs are never constrained to make the log green.
- Oracle hashing remains byte-exact. `.gitattributes` reproduces kickoff CRLF
  bytes on Linux rather than weakening verification with newline normalization.
- CI runs `deno test test.js`, not broad `deno task test`, because Deno 2 would
  discover generated npm and port-owned Node tests.
- Luminance parity needs an exact finite-domain solution. Heuristic ULP
  correction is ruled out because channel deltas have both signs.
- Keep micro-commits: regression/root-cause fixes, evidence artifacts, and
  debug/archive docs stay separately revertible.

</decisions_made>

<blockers>

- Technical blocker: exact V8/Go power rounding mismatch in `Color.Luminance`.
- Expected red checks until fixed:
  - `go test ./tinycolor -run TestReadabilityMatchesJavaScriptPrecision`
  - operations corpus has the new readability case as its mismatch
  - `node --test fuzz/harness.test.mjs` fails only its zero-divergence test
  - seed-1 one-second broad run previously produced 12,291 cases / 388
    divergences; first was the readability case above.

</blockers>

<uncommitted_wip>

- `fuzz/harness.mjs` — persistent broad differential harness.
- `fuzz/harness.test.mjs` — PRNG/options/generator/end-to-end tests.
- `fuzz/README.md` — reproduction and claim rules.
- `src/tinycolor/operations_test.go` — exact readability RED regression and
  temporary numeric logging.
- `compat/cases/operations.jsonl` — adapter RED regression row.
- `.planning/debug/fuzz-readability-precision.md` — active root-cause evidence.
- `src/cmd/pow-debug/main.go` — temporary channel-bit diagnostic.

These files are preserved in the WIP handoff commit. The temporary logging and
`pow-debug` command may be removed only after the exact solution is verified;
do not delete them before the next agent reads the evidence.

</uncommitted_wip>

<verification_snapshot>

- GitHub Actions: PASS — run 30673847990.
- Local Deno: PASS — 45 passed, 0 failed, 1 ignored.
- Oracle hash verifier: PASS — 3 files.
- Fixed corpus through saturation commit: operations 70/70.
- Current WIP: intentionally RED only for unresolved readability precision and
  fuzz zero-divergence.
- No background server or watcher is running; all subagents were stopped.

</verification_snapshot>

<next_action>

Start with:

```text
$gsd-resume-work
```

Then read this file and `.planning/debug/fuzz-readability-precision.md`. Finish
the exact luminance transfer fix with the existing RED tests before changing
the fuzz harness. The first mandatory verification after the fix is:

```powershell
$env:GOCACHE = (Resolve-Path '.cache\go-build').Path
go -C src test ./tinycolor -run TestReadabilityMatchesJavaScriptPrecision -count=1
node compat/run.mjs compat/cases/operations.jsonl
node fuzz/harness.mjs --duration 1 --seed 1
```

</next_action>
